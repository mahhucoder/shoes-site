import React, { memo } from 'react';
import PropTypes from 'prop-types';

const BaseDropdown = memo((props) => {
    return (
        <div>
            
        </div>
    );
});

export default BaseDropdown;